---
 - name: This playbook will create an user
   hosts: webservers

   tasks:
           - name: To create an user named jenkins
             user:
                     name: jenkins
                     uid: 4000
                     shell: /bin/bash
                     state: present
